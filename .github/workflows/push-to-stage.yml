name: Java Build & Release

on:
  push:
    branches:
      - main
      - CLIENT-3895-upgrade-shared-wf-version
  workflow_dispatch:
  #   - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  build-stage:
    name: Build stage
    uses: ./.github/workflows/build-release.yml
    secrets: inherit