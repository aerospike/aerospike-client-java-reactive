name: Java Build & Release

on:
  push:
    branches:
      - stage
      - CLIENT-3877-fix-fetching-tag
    tags:
      - 'v*'
  workflow_dispatch:
  #   - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  build-stage:
    name: Build stage
    uses: ./.github/workflows/build-release.yml
    secrets: inherit
